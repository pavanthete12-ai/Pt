"use client";
import {useEffect,useMemo,useState} from "react";
const symbols=["NIFTY 50","BANKNIFTY","RELIANCE","TCS","INFY","HDFCBANK"];
export default function Home(){
 const [symbol,setSymbol]=useState("NIFTY 50"); const [live,setLive]=useState(false); const [connected,setConnected]=useState(false); const [last,setLast]=useState<number|null>(null); const status=useMemo(()=>connected?"LIVE STREAM CONNECTED":live?"LIVE GATE ARMED":"PAPER MODE",[connected,live]);
 useEffect(()=>{ if(!live)return; const base=process.env.NEXT_PUBLIC_QUANTPULSE_WS_URL; if(!base)return; const ws=new WebSocket(base); ws.onopen=()=>setConnected(true); ws.onclose=()=>setConnected(false); ws.onerror=()=>setConnected(false); ws.onmessage=e=>{try{const p=JSON.parse(e.data);if(p.type==="candle"&&p.symbol===symbol)setLast(Number(p.close));}catch{}}; ws.onopen=()=>{setConnected(true);ws.send(JSON.stringify({type:"subscribe",symbol,timeframe:"5m"}));}; return()=>{ws.close();setConnected(false)};},[live,symbol]);
 return <main><header><div><b>⚡ QuantPulse</b><span> Market Intelligence Terminal</span></div><div className="status">{status}</div></header>
 <section className="hero"><div><p className="eyebrow">TRADING PLATFORM</p><h1>Market intelligence, signals & risk — in one terminal.</h1><p className="muted">Provider-fed OHLCV streaming, deterministic technical analysis, pattern detection and risk controls. Paper trading remains the default.</p></div><div className="gate"><button onClick={()=>setLive(!live)}>{live?"Disable live stream":"Connect live stream"}</button><small>Live execution is separately locked. A market-data WebSocket URL must be configured by the deployment.</small></div></section>
 <nav>{symbols.map(s=><button className={s===symbol?"active":""} onClick={()=>setSymbol(s)} key={s}>{s}</button>)}</nav>
 <section className="grid"><article className="chart"><div className="cardhead"><div><strong>{symbol}</strong><div className="muted">5m • provider-fed market structure</div></div><div className="liveDot">{connected?"● LIVE":"○ DISCONNECTED"}</div></div><div className="chartbox"><div className="price">{last?last.toFixed(2):"—"}</div><div className="empty">{connected?"Waiting for the next validated OHLCV candle…":"Connect an authorized real-time market-data provider to stream candles here."}</div></div></article>
 <aside><div className="signal"><p>AI FUSION SIGNAL</p><h2>WAITING FOR ANALYSIS</h2><div className="confidence">—</div><span>Confidence is model strength, not profit probability.</span></div><div className="risk"><p>RISK ENGINE</p><div>Entry <b>{last?last.toFixed(2):"—"}</b></div><div>Stop loss <b>—</b></div><div>Target <b>—</b></div><div>R:R <b>2.0</b></div></div></aside></section>
 <section className="cards"><div><b>Trend Engine</b><span>EMA / regime analysis</span></div><div><b>Pattern Scanner</b><span>Doji / hammer / rejection / impulse</span></div><div><b>News Intelligence</b><span>Provider adapter ready</span></div><div><b>Paper Trading</b><span>Persistent orders + P&amp;L</span></div></section><footer>QuantPulse is analytical software. Signals are not guaranteed returns or profit probabilities. Real-money execution remains locked until security, broker authorization and risk controls are independently verified.</footer></main>
}